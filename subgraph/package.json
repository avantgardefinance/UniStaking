{
  "name": "subgraph",
  "private": true,
  "scripts": {
    "codegen": "graph codegen",
    "lint": "eslint \"src/**/*.ts\"",
    "format": "pnpm lint --fix",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ uniswap/staking",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 uniswap/staking",
    "create-local": "graph create --node http://localhost:8020/ uniswap/staking",
    "remove-local": "graph remove --node http://localhost:8020/ uniswap/staking"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.67.3",
    "@graphprotocol/graph-ts": "0.32.0"
  }
}
