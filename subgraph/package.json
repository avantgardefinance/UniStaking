{
  "name": "subgraph",
  "private": true,
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ enzymefinance/uni-staking",
    "deploy-sepolia": "graph deploy --node https://api.thegraph.com/deploy/ enzymefinance/uni-staking-sepolia"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.69.1",
    "@graphprotocol/graph-ts": "^0.34.0"
  }
}
